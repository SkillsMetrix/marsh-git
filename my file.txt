import { Component, ComponentFactoryResolver, ViewChild, ViewContainerRef } from '@angular/core';
import { ChildComponent } from '../child/child.component';

@Component({
  selector: 'app-parent',
  templateUrl: './parent.component.html',
  styleUrl: './parent.component.css'
})
export class ParentComponent {

  componentRef:any
@ViewChild('show',{read:ViewContainerRef})entry:ViewContainerRef

  constructor( private resolver:ComponentFactoryResolver){}

  createComponent(userMessage){
   const factory= this.resolver.resolveComponentFactory(ChildComponent)
   this.componentRef=this.entry.createComponent(factory)
   this.componentRef.instance.message=userMessage

  }

}

---------



<template #show></template>


<hr>

<button (click)="createComponent('Message-1 to child')">Button-1</button>

<button (click)="createComponent('Message-2 to child')">Button-2</button>
